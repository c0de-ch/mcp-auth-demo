node_modules/
dist/
coverage/
.env
.env.*
!.env.example
.mcp-auth/
*.log
.DS_Store
# generated certificates (mTLS example) — regenerate with the provided script
examples/**/certs/*.pem
examples/**/certs/*.key
examples/**/certs/*.crt
examples/**/certs/*.srl
# python
.venv/
__pycache__/
*.pyc
# playwright artefacts
test-results/
playwright-report/
screenshots/
# rendered from keycloak/realm-mcp.template.json by scripts/kc.sh
keycloak/.generated/

# course audio (scripts/render-audio.sh): the venv, the ~120 MB voice model and the
# rendered speech. 61 minutes of WAV is ~155 MB — publish it, do not commit it.
/.audio/
/audio/
