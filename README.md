# mcp-auth-demo
MCP server + client examples for many authentication approaches: API key, JWT, embedded OAuth 2.1, Keycloak (resource server, client credentials, proxy, introspection, token exchange), mTLS, auth gateway
